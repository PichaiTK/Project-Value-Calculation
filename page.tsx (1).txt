"use client"

import { cn } from "@/lib/utils"

import { useState } from "react"
import { VideoPlayer } from "@/components/video-player"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Play, Lock, Tv, ListMusic } from "lucide-react"
import { ScrollArea } from "@/components/ui/scroll-area"

const channels = [
  {
    id: "1",
    name: "Global News 24/7",
    category: "News",
    isPremium: false,
    src: "https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8",
    poster: "/news-studio.jpg",
  },
  {
    id: "2",
    name: "Action Cinema",
    category: "Movies",
    isPremium: true,
    src: "https://bitdash-a.akamaihd.net/content/sintel/hls/playlist.m3u8",
    poster: "/action-movie.jpg",
  },
  {
    id: "3",
    name: "Nature Wild",
    category: "Documentary",
    isPremium: false,
    src: "https://test-streams.mux.dev/pts_6sec/master.m3u8",
    poster: "/nature-wildlife.jpg",
  },
]

export default function TVPage() {
  const [activeChannel, setActiveChannel] = useState(channels[0])

  return (
    <div className="p-8 max-w-7xl mx-auto space-y-8">
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-3xl font-bold text-primary tracking-tight">Streaming Hub</h1>
          <p className="text-muted-foreground mt-1">Watch live channels and premium content</p>
        </div>
        <Badge variant="outline" className="px-4 py-1.5 border-primary/30 text-primary">
          <Tv className="w-4 h-4 mr-2" />
          LIVE NOW
        </Badge>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
        <div className="lg:col-span-3 space-y-6">
          <VideoPlayer src={activeChannel.src} poster={activeChannel.poster} />

          <Card className="border-border/50 bg-card/50 backdrop-blur-sm">
            <CardHeader className="flex flex-row items-center justify-between">
              <div>
                <CardTitle className="text-2xl">{activeChannel.name}</CardTitle>
                <p className="text-muted-foreground mt-1">{activeChannel.category} • HD • 60 FPS</p>
              </div>
              {activeChannel.isPremium && (
                <Badge className="bg-amber-500/10 text-amber-500 border-amber-500/20">
                  <Lock className="w-3 h-3 mr-1" />
                  PREMIUM
                </Badge>
              )}
            </CardHeader>
            <CardContent>
              <p className="text-sm leading-relaxed text-muted-foreground">
                Currently broadcasting the latest updates and high-quality entertainment from across the globe. Join the
                conversation in the chat or upgrade to Premium for ad-free viewing.
              </p>
            </CardContent>
          </Card>
        </div>

        <div className="space-y-6">
          <Card className="h-full border-border/50 bg-card/50 backdrop-blur-sm flex flex-col">
            <CardHeader className="border-b border-border/50">
              <CardTitle className="text-lg flex items-center gap-2">
                <ListMusic className="w-5 h-5 text-primary" />
                Channel List
              </CardTitle>
            </CardHeader>
            <ScrollArea className="flex-1 p-2">
              <div className="space-y-2">
                {channels.map((channel) => (
                  <button
                    key={channel.id}
                    onClick={() => setActiveChannel(channel)}
                    className={cn(
                      "w-full flex items-start gap-3 p-3 rounded-lg transition-all text-left group",
                      activeChannel.id === channel.id
                        ? "bg-primary/10 border border-primary/30"
                        : "hover:bg-muted/50 border border-transparent",
                    )}
                  >
                    <div className="relative w-20 aspect-video rounded overflow-hidden bg-black shrink-0">
                      <img
                        src={channel.poster || "/placeholder.svg"}
                        alt={channel.name}
                        className="w-full h-full object-cover opacity-60 group-hover:opacity-100 transition-opacity"
                      />
                      <div className="absolute inset-0 flex items-center justify-center">
                        <Play className="w-4 h-4 text-white fill-white opacity-0 group-hover:opacity-100 transition-opacity" />
                      </div>
                    </div>
                    <div className="min-w-0">
                      <p className="font-medium text-sm truncate">{channel.name}</p>
                      <p className="text-xs text-muted-foreground">{channel.category}</p>
                    </div>
                  </button>
                ))}
              </div>
            </ScrollArea>
          </Card>
        </div>
      </div>
    </div>
  )
}
